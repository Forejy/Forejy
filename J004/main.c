#include <stdio.h>
#include <time.h>

//int		ft_iterative_power(int nb, int power);
//int		ft_recursive_power(int nb, int power);

int main(void)
{	int time = 0;
//	printf("%d\n", (ft_iterative_power(-10, 200)));
//	printf("%d", (ft_recursive_power(2, 10)));
	time = clock();
	printf("%dms", time);
}

